# --- Web framework (all services) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # required for FastAPI's UploadFile/File(...) form handling -
                           # missing this causes a runtime error the moment an image is uploaded

# --- Image classifier (app.py / test.py) ---
torch>=2.2.0
torchvision>=0.17.0
pillow>=10.0.0

# --- Track B numeric API (main.py / utils.py) ---
pandas>=2.0.0
scikit-learn>=1.4.0        # not directly imported in main.py/utils.py, but required to
                            # unpickle the joblib-saved RandomForest models
joblib>=1.3.0

# --- Gateway (gateway.py) ---
httpx>=0.27.0